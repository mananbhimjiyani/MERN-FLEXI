export const calculateCompatibilityScore = (
  preferences1: any,
  preferences2: any
): number => {
  // This is a placeholder function. You should implement your own logic
  // to calculate compatibility based on user preferences.
  // For now, we'll return a random score between 0 and 100.
  return Math.floor(Math.random() * 101);
};
